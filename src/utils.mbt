///|
/// Computes the successor universe level.
///
/// Parameters:
///
/// * `level`: current universe index (0 for `Type0`).
///
/// Returns the next level (`level + 1`).
fn level_succ(level : Int) -> Int {
  level + 1
}
